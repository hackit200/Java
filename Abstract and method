/*Program Title:- Create abstract class Shape which has instance variables side, area and
perimeter And methods calculateArea(), calculatePerimeter() as abstract methods and display()
as concrete method. Write subclasses which extend Shape class like Triangle, Rectangle, Circle,
Cube and Squere and override abstract methods and display methods in subclass take instance
variable if needed as per the formula. And use parameterized constructor to initialize instance
variables using “this” reference variable. Write Test class and Create a reference variable of
Shape which will hold the objects of all the sub classes and calculate respective area, perimeter
and display the results.

Developer:-SSM
Version:-v1.0
Program Code:-005

*/
 import java.util.*;
 abstract class Shape
{
protected float side;
protected double area;
protected double perimeter;
Shape()
{
side = 0;
}
Shape(float newSide)
{
side = newSide;
}
public abstract void calcaulateArea();
public abstract void calcaulatePerimeter();
public void display()
{
System.out.println("\nSide: "+side+"\nArea: "+area+"\nPerimeter: "+perimeter);
}
}
class Triangle extends Shape
{
 Triangle()
{
super.side = 0;
}
Triangle(float newSide)
{
super.side = newSide;
}
public void calcaulateArea()
{
area = (side*side)*1/2;
}
public void calcaulatePerimeter()
{
perimeter = side+side+side;
}
}
class Rectangle extends Shape{
 Rectangle(){
super.side = 0;
}
Rectangle(float newSide)
{
super.side = newSide;
}
public void calcaulateArea()
{
area = side*side;
}
public void calcaulatePerimeter()
{
perimeter = 2*(side*side);
}
}
class Square extends Shape{
Square(){
super.side = 0;
}
Square(float newSide)
{
super.side = newSide;
}
public void calcaulateArea()
{
area = side*side;
}
public void calcaulatePerimeter()
{
perimeter = 4*side;
}
}
class Cube extends Shape{
 Cube(){
super.side = 0;
}
Cube(float newSide)
{
super.side = newSide;
}
public void calcaulateArea()
{
area = 6*(side*side);
}
public void calcaulatePerimeter()
{
perimeter = 12*side;
}
}
class Circle extends Shape{
 Circle(){
super.side = 0;
}
Circle(float newSide)
{
super.side = newSide;
}
public void calcaulateArea()
{
area = 3.14*(side*side);
}
public void calcaulatePerimeter()
{
perimeter = 2*3.14*side;
}
}
class Shape
{
public static void main(String arga[]){
Shape s;
s = new Triangle(10);
System.out.println("\nTriangle");
s.calcaulateArea();
s.calcaulatePerimeter();
s.display();
System.out.println("\nRectangle");
s = new Rectangle(10);
s.calcaulateArea();
s.calcaulatePerimeter();
s.display();
System.out.println("\nSquare");
s = new Square(10);
s.calcaulateArea();
s.calcaulatePerimeter();
s.display();

System.out.println("\nCube");
s = new Cube(10);
s.calcaulateArea();
s.calcaulatePerimeter();
s.display();
System.out.println("\nCircle");
s = new Circle(10);
s.calcaulateArea();
s.calcaulatePerimeter();
s.display();
}
}
